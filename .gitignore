*.py
# 全站 Python 流水线脚本是采集/渲染核心，必须入库跟踪（纯代码、无密钥）。
# 原 `*.py` 一刀切导致 promote_sources/collect_sources/meme_radar/qc_leakage 等核心脚本
# 无法入库 —— 修复（扩源/防漏采）在本机生效、下次部署却丢失，与 sources.toml 同类问题。
!*.py
*.pyc
__pycache__/
_publish/
backup_*/
*.md
*.toml
# 信源注册表是采集流水线的源头，必须入库跟踪（RSS URL 不含密钥）
!sources.toml
sources_*.json
collectors/
*.log
.DS_Store
Thumbs.db
dist-gamepulse-radar/

# 一次性调试/迁移脚本不入库（如 check_wc_age / clean_wc_stale / backfill）
.workbuddy/check_*.py
.workbuddy/clean_*.py
.workbuddy/backfill_wc_pubdate.py
